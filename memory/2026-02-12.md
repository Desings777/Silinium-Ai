# Memory - 2026-02-12

## Bybit Connector Skill Created

User: Zigilx0
Purpose: Read-Only Bybit API access for trading data (futures/scalping)

### Credentials & Security
- API Key: `TMBYJjyISWzuAWZhvQ`
- Auth: RSA Self-generated (public + private keys)
- IP Whitelist: `15.229.157.209` (VPS)
- Permissions: Read-Only (no trading)
- Files stored:
  - `skills/bybit-connector/config/credentials.json`
  - `skills/bybit-connector/config/private-key.pem` (chmod 600)

### Skill Capabilities
- `price` – current price, 24h change
- `klines` – OHLC candles (1m, 5m, etc.)
- `orderbook` – market depth
- `trades` – recent trades
- `balance` – pending fix (signature issue)

### Categories Supported
- spot
- linear (USDT futures) – preferred for scalping
- inverse (USD futures)

### User Trading Profile
- Trades on Bybit
- Uses **futures** (linear, USDT)
- Scalping timeframes: **1m, 5m**
- Starting capital: ~$100
- Operations with **lots** (leverage)
- Preferred pairs: originally asked about XAU/USD (TradFi) but shifted to SOL/USDT for futures

### Commands Example
```bash
node index.js price --pair SOL/USDT --category linear
node index.js klines --pair SOL/USDT --interval 1m --category linear --limit 20
node index.js orderbook --pair SOL/USDT --category linear --depth 20
```

### Next Steps Requested
- Possibly extend to automated alerts
- May later add trading execution (Write permissions) after trust/validation
- Fix balance command

### Notes
- Bybit does not expose TradFi (XAU/USD+) via public API – user switched to crypto futures
- Skill tested successfully with SOL/USDT linear
- Recommend low leverage (1x-5x) for $100 account
- Keep credentials secure; rotate keys periodically